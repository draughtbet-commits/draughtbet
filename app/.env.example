BACKEND_URL=http://localhost:3000
# Keep legacy while running the current backend. Switch to v2 only when the
# documented /api/v1 and realtime V2 contracts are enabled server-side.
BACKEND_CONTRACT=legacy
